# Agents 流程规则

本文件定义本项目开发过程中的强制流程规范，所有 Agent（含本项目的 AI 编码助手）在改动代码时必须遵守。

## 规则

### 1. 每次改动完成之后，都必须创建一个对应的 git commit

- 每完成一次改动（功能、修复、重构、文档、配置变更等），都必须对应的创建一个 git commit。
- 目的：便于后续追踪变更历史、定位问题，并可在任何时刻回滚到任意版本。
- 要求：
  - commit 信息应清晰、描述性，遵循项目约定（如 `feat:` / `fix:` / `chore:` 等前缀）。
  - 一个 commit 聚焦一个逻辑变更，避免把不相干的改动混在一起提交。
  - 不要提交敏感信息或无关生成物。

### 2. 每次改动后都必须先编写或更新相关测试，在交给用户前，确保所有测试和验证全部通过

- 每次改动之后，都要先为本次改动编写或更新对应的测试用例。
- 在把改动交给用户之前，必须运行全部相关测试与验证，并确保全部通过。
- 要求：
  - 新功能：补充覆盖该功能的测试用例。
  - 修改/修复：更新或补充受影响的测试，确保通过。
  - 提交前完成测试：测试全部通过后，才算完成本次改动，方可进入规则 1 的提交流程。
  - 若测试失败，先修复代码或测试，直到全部通过为止。

## 执行顺序

对于任意改动，按以下顺序执行：

1. 编写 / 更新测试。
2. 实现或修改代码，并使测试通过。
3. 运行所有相关测试与验证，确认全部通过。
4. 创建对应 git commit，提交本次改动。
